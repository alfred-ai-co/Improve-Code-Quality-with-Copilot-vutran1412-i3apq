[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = --strict-markers
markers =
    asyncio: mark a test as an asyncio test
    anyio: mark tests using anyio for async support
